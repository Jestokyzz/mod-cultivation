# Семь новых умений: иконки прежнего стиля

Кандидат `v1.3.0-candidate4`, 31.08.2026. Статус: локальный тестовый кандидат, визуальная приёмка в игре ожидается. Не является опубликованным или принятым релизом.

По подтверждению пользователя добавлены по две иконки для семи умений: Подлый трюк, Удар в спину, Ослепление, Безопасное падение, Ошеломление, Призрачный удар, Отравляющий укол. Небожитель использует холодное голубое оформление Sage, Ша — пурпурное Demon. Исходный рисунок сохранён; используется прежняя принятая рамка +2 px, без дополнительного утолщения поверх неё.

## Источники и воспроизводимость

Генератор: `C:\Solo WotLK\tools\icon_pipeline\build_content_locked_seven.py`. Он вызывает прежние `build_content_locked.py` и `build_content_locked_full.py`, не запускает очистку полного набора и не использует генеративную перерисовку. Исходные BLP извлечены из проверенных официальных MPQ; пути и SHA-256 записаны в манифесте изображений.

PNG, исходники, QA и предпросмотры: `C:\Solo WotLK\artifacts\rogue_pw_icons\content_locked_seven_v1`. Папки `sage` и `demon` содержат по семь изображений.

Проверки:

- 14/14 изображений прошли CONTENT_LOCK и FILTER_ONLY проверки установленного конвейера.
- 4 контрольных повторных генерации прежних иконок побайтно совпали с утверждёнными PNG.
- Все 78 прежних PNG и все 78 прежних BLP сохранены побайтно.
- `SpellIcon` 6178–6191 добавлены без изменения прежних записей.
- Изменено только поле SpellIconID (133) у 42 вариантов заклинаний, покрывающих все ранги семи умений. Стандартные заклинания, тултипы, механика и строки DBC не изменены.
- 26 основных, 13 schema-3 и 4 новых регрессионных теста пройдены. Аудит: 600 тултипов, 342 привязки иконок, 42 отображаемые пассивные записи, 19 узлов талантов; ошибок нет.
- Владелец текстур `patch-ruRU-A.MPQ` собран из полного чистого staging с 92 файлами, каждый извлечён обратно и сверен по SHA-256. Стандартные пути текстур не заменяются.
- Владелец DBC `patch-ruRU-Z.MPQ` собран из проверенной копии прежнего владельца с точечными заменами и обратным извлечением. Пересечений владельцев не обнаружено; FrameXML не менялся.

## Установка и откат

Пакет: `client_patch/build/v1.3.0-candidate4`; SHA-256 находятся в `manifest.json`. Поддерживаются только `C:\Solo WotLK\test-client\20260831-rogue-paths-v3` и соответствующий `test-server\20260831-rogue-paths-v3`.

Установщик этой ревизии: `C:\Solo WotLK\work\rogue-paths-celestial-v3\deploy_seven_icons.ps1`. Он меняет только два MPQ и серверные Spell.dbc/SpellIcon.dbc, предварительно проверяет полный backup и пробное восстановление на F:, затем перезапускает только worldserver v3. SQL, EXE, AddOn, Cache, WTF, основной клиент и открытый v2 не изменяются.

Проверенная копия до установки: `F:\JestokyCraft Backups\rogue-paths\pre-change-seven-icons-install-20260831T193117`. Для отката остановить именно клиент/worldserver v3, сверить SHA-256 резервных файлов с manifest.json, восстановить четыре перечисленных файла в их Source-пути и снова сверить SHA-256. Затем запустить worldserver v3. Нельзя восстанавливать их в основной клиент или другой тестовый сервер.

## Известные ограничения

Предпросмотр проверен в 64 и 32 px; проверка в реальном UI клиента v3 ещё не выполнена. Эти результаты не подтверждают визуальную приёмку или завершение остальных задач schema-3. Открытый v2 не получит эти иконки: изменения установлены только в v3.
